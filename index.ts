// явная или выводимая типизация

const contry: string = "Japan"; // явная
const mainCountry = "Ukraine"; //понимает автоматически

//Структурная типизация:

type User = {
  firstName: string;
  secondName: string;
};

type Person = {
  firstName: string;
  secondName: string;
};
